<Window x:Class="WarThunderSkinManager.Views.MessageDialogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        ShowInTaskbar="False"
        Width="460" SizeToContent="Height" MaxHeight="640"
        WindowStyle="None" ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        FontFamily="Segoe UI, Microsoft YaHei UI"
        Foreground="{StaticResource BrushText}"
        Background="{StaticResource BrushBackground}">
    <Border BorderThickness="1" BorderBrush="{StaticResource BrushBorder}" Background="{StaticResource BrushBackground}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- 标题栏（可拖动） -->
            <Grid Grid.Row="0" Background="{StaticResource BrushCanvas}"
                  MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <TextBlock x:Name="HeaderText" Margin="14,0" VerticalAlignment="Center"
                           FontWeight="SemiBold" Foreground="{StaticResource BrushText}"
                           TextTrimming="CharacterEllipsis"/>
                <Button Style="{StaticResource TitleBarCloseButton}" Content="&#xE8BB;"
                        HorizontalAlignment="Right" Click="Cancel_Click"/>
            </Grid>

            <!-- 图标 + 正文 -->
            <Grid Grid.Row="1" Margin="20,16,20,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock x:Name="IconText" FontFamily="{StaticResource IconFont}" FontSize="26"
                           VerticalAlignment="Top" Margin="0,0,14,0"/>
                <ScrollViewer Grid.Column="1" MaxHeight="330" VerticalScrollBarVisibility="Auto">
                    <TextBlock x:Name="MessageText" TextWrapping="Wrap" FontSize="13" LineHeight="21"
                               Foreground="{StaticResource BrushTextSecondary}"/>
                </ScrollViewer>
            </Grid>

            <!-- 选填的「下次不再提醒」+ 按钮 -->
            <StackPanel Grid.Row="2" Margin="20,16,20,18">
                <CheckBox x:Name="NoticeCheck" Margin="2,0,0,14" Visibility="Collapsed"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button x:Name="CancelButton" Style="{StaticResource SecondaryButton}" Click="Cancel_Click"/>
                    <Button x:Name="PrimaryButton" Margin="8,0,0,0" Click="Primary_Click"/>
                </StackPanel>
            </StackPanel>
        </Grid>
    </Border>
</Window>
